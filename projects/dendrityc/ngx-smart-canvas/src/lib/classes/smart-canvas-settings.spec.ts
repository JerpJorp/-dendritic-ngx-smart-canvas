import { SmartCanvasSettings } from './smart-canvas-settings';

describe('SmartCanvasSettings', () => {
  it('should create an instance', () => {
    expect(new SmartCanvasSettings()).toBeTruthy();
  });
});
